name = "remix"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npx.cmd prisma generate"
dev = "npx.cmd prisma migrate deploy && npm.cmd exec remix vite:dev"
